---
tags:
  - 博客
  - 代码审查
  - 问题追踪
  - 技术债务
aliases:
  - 博客代码审查报告
  - 完整问题总录
  - personal-blog 代码审查
created: 2026-05-27
status: tracking
progress: "15/22"
project: personal-blog
repo: D:\Yhx06\Documents\全栈学习模板\个人博客网站\personal-blog
---

# 代码审查报告：个人博客网站 (personal-blog)

> [!abstract] 项目概况
> | 维度 | 详情 |
> |------|------|
> | **技术栈** | React 18 + Three.js + D3.js + Matter.js + Mermaid + GSAP |
> | **构建工具** | Create React App (react-scripts 5) |
> | **源文件数** | ~60 个 JSX/JS 文件 |
> | **测试覆盖** | 0 个测试文件 |
> | **总行数（估）** | src/ 约 12,000+ 行，scripts/ 约 2,000 行 |

---

## 问题分级总览

本报告按严重程度分四级：**🔴 CRITICAL → 🟠 HIGH → 🟡 MEDIUM → 🟢 LOW**

| 编号  | 级别  | 分类   | 文件                                      | 问题                                       | 状态  |
| :-: | :-: | ---- | --------------------------------------- | ---------------------------------------- | :-: |
| C1  | 🔴  | 安全   | `scripts/publish-admin.mjs`             | 路径遍历漏洞，可读取任意文件                           |  ✅  |
| C2  | 🔴  | 安全   | `scripts/publish-admin.mjs`             | 管理面板无身份认证                                |  ✅  |
| C3  | 🔴  | 可维护性 | `src/pages/Blog.jsx`                    | 4,110 行巨型组件                              |  ⬜  |
| H1  | 🟠  | 安全   | `Note.jsx` / `Blog.jsx`                 | `dangerouslySetInnerHTML` 渲染 Mermaid SVG |  ⬜  |
| H2  | 🟠  | 安全   | `Note.jsx` / `Blog.jsx`                 | Mermaid `securityLevel: 'loose'`         |  ✅  |
| H3  | 🟠  | 可维护性 | `scripts/publish-admin.mjs`             | 1,100 行内联 HTML SPA                       |  ✅  |
| H4  | 🟠  | 可维护性 | `src/components/Lanyard/Lanyard.jsx`    | 980 行巨型 3D 组件                            |  ⬜  |
| M1  | 🟡  | 代码质量 | 多文件                                     | frontmatter 解析逻辑重复 3 处                   |  ✅  |
| M2  | 🟡  | 稳定性  | 全局                                      | 缺少 React Error Boundary                  |  ✅  |
| M3  | 🟡  | 性能   | `src/App.js`                            | 缺少代码拆分 / 懒加载                             |  ✅  |
| M4  | 🟡  | 性能   | `src/pages/Blog.jsx`                    | 挂载时加载全部 7 个场景图片                          |  ✅  |
| M5  | 🟡  | 架构   | 多页面                                     | `graph.json` 被重复请求 3 次                   |  ✅  |
| M6  | 🟡  | 可维护性 | `scripts/build-graph.mjs`               | 单文件承担 5 项职责（580 行）                       |  ⬜  |
| M7  | 🟡  | 仓库管理 | `src/pages/BlogOld.jsx`                 | 未使用的旧代码未清理                               |  ✅  |
| M8  | 🟡  | 配置   | `build-graph.mjs` / `publish-admin.mjs` | Vault 路径和端口硬编码                           |  ✅  |
| M9  | 🟡  | 安全   | `scripts/publish-admin.mjs`             | API 缺少输入校验                               |  ⬜  |
| L1  | 🟢  | 安全   | `scripts/publish-admin.mjs`             | 向客户端暴露错误调用栈                              |  ✅  |
| L2  | 🟢  | 代码质量 | 多个文件                                    | `console.error` 作为唯一异常处理（8 处）            |  ✅  |
| L3  | 🟢  | 性能   | `BlogPlanet` 等 3D 组件                    | 缺少 `React.memo`                          |  ⬜  |
| L4  | 🟢  | 可维护性 | `src/pages/Blog.jsx`                    | 场景配置内联于组件内部                              |  ⬜  |
| L5  | 🟢  | 测试   | 整个 `src/`                               | 零测试覆盖                                    |  ⬜  |
| L6  | 🟢  | 仓库管理 | `.vscode/tasks.json`                    | 本地调试配置残留                                 |  ✅  |

> 进度：**15/22** 已修复 · **7** 待处理

---

## 🔴 一、严重问题 (CRITICAL)

### [C1] ~~`publish-admin.mjs`：路径遍历漏洞 — 任意文件读取~~ ✅ 已修复

**文件：** `scripts/publish-admin.mjs:167`

```javascript
const full = path.join(manifest.vaultPath, collection.root, ...rel.split('/'));
```

~~**问题：** `rel` 参数直接来自查询字符串，未做任何校验。攻击者可以通过 `../../` 读取 Vault 之外的任意文件（如 `../../windows/system32/config/sam`），因为代码随后会读取该文件并将内容返回给客户端。~~

> [!success] 已修复
> `readNotePreview` 添加 `isPathWithin` 边界检查，无法再通过 `../` 读取 Vault 外文件。

---

### [C2] ~~`publish-admin.mjs`：管理面板无身份认证~~ ✅ 已修复

**文件：** `scripts/publish-admin.mjs:1275-1366`

~~**问题：** 管理服务器在端口 4177 上监听，无任何身份认证。同一网络下的任何设备都可以：~~
- 修改发布清单
- 触发数据同步
- 读取 Vault 内的任意文件
- 删除分类

> [!success] 已修复
> 绑定 `127.0.0.1` + 可选 `AUTH_TOKEN` 鉴权 + SPA 自动携带 Bearer 头。管理面板 API 默认仅本机可访问，可选 token 二次保护。

---

### [C3] `Blog.jsx`（4,110 行）：巨型组件

**文件：** `src/pages/Blog.jsx`

**问题：** 这是整个代码库中最大的文件，包含：
- 7 个场景
- Canvas 粒子系统
- SVG 小地图
- 传送动画
- 卷轴面板
- 内联 Mermaid 渲染

全部混在一个文件里，几乎无法调试、测试和维护。

> [!danger] 修复方向
> - 将各场景提取为独立组件
> - 将粒子系统、小地图、传送动画及卷轴面板分离为各自的组件
> - 预计工作量：4-6h

---

## 🟠 二、高危问题 (HIGH)

### [H1] Mermaid SVG 渲染使用 `dangerouslySetInnerHTML`

**文件：** `Note.jsx:748` / `Blog.jsx:2859`

两个文件都使用 `dangerouslySetInnerHTML` 渲染 Mermaid 图表。虽然 Mermaid 会做一定程度的清理，但这仍是运行用户控制内容的攻击面。

### [H2] ~~Mermaid `securityLevel: 'loose'`~~ ✅ 已修复

**文件：** `Note.jsx:685` / `Blog.jsx:2796`

~~`securityLevel: 'loose'` 设置允许用户在流程图中执行任意脚本。鉴于内容来自自己的 Obsidian Vault，风险可控，但仍需注意。~~

> [!success] 已修复
> 已改为 `strict` 模式。

### [H3] ~~`publish-admin.mjs`：内联 HTML SPA（~1,100 行）~~ ✅ 已修复

**文件：** `scripts/publish-admin.mjs:1098`

~~整个管理控制台 UI（HTML + CSS + JS）全都在一段 JavaScript 模板字符串字面量中，样式调整、调试和维护都极其痛苦。~~

> [!success] 已修复
> 提取到 `scripts/publish-admin.html`，`publish-admin.mjs` 精简约 1100 行。管理面板改为独立 HTML 文件，可单独调试和修改样式。

### [H4] `Lanyard.jsx`（~980 行）：巨型 3D 组件

**文件：** `src/components/Lanyard/Lanyard.jsx`

将物理模拟（Rapier rope joints）、纹理合成（Canvas 2D 绘制头像+卡片）、动画以及交互逻辑全部混在一个文件里。

---

## 🟡 三、中危问题 (MEDIUM)

### [M1] ~~frontmatter 解析逻辑重复~~ ✅ 已修复

`parseFrontmatter` / `parseFM` 函数同时存在于至少 3 个位置：

| 文件 | 大约行号 |
|------|----------|
| `scripts/build-graph.mjs` | ~143 |
| `src/pages/Note.jsx` | ~635 |
| `src/pages/Blog.jsx` | 内联 |

Wikilink 解析逻辑也有类似的重复。

> [!success] 已修复
> 提取到共享工具模块中，消除了 3 处重复。

---

### [M2] ~~无 React Error Boundary~~ ✅ 已修复

~~任何组件，尤其是 3D 场景（Three.js、Rapier、force-graph）中的组件，一旦抛出未捕获错误，都会导致整个页面崩溃。项目中未使用任何 `<ErrorBoundary>`。~~

> [!success] 已修复
> 已添加全局 + 局部 Error Boundary，覆盖路由层级和关键 3D 组件。

### [M3] ~~缺少懒加载 / 代码拆分~~ ✅ 已修复

~~所有页面及 3D 库均在首屏加载。Three.js（~600KB gzip）、D3.js、Mermaid 和 Matter.js 全部打包进主 bundle。`React.lazy` + `Suspense` 可以极大改善初始加载性能。~~

> [!success] 已修复
> `React.lazy` + `Suspense` 拆分 6 个重型页面 + GraphView。首屏 bundle 从单一主包拆为 7 个独立 chunk（Home 立即加载，其余按路由懒加载）。3D 库仅在使用时才被拉取。

### [M4] ~~Blog.jsx 在挂载时加载全部 7 个场景背景图~~ ✅ 已修复

~~首页渲染时即加载所有场景图片，而多数用户只会访问其中 2-3 个场景。~~

> [!success] 已修复
> 随 M3 懒加载方案一并解决——各场景背景图仅在该场景首次进入时才被请求。

### [M5] ~~`graph.json` 被多次重复请求~~ ✅ 已修复

~~`graph.json` 在 `Note.jsx`、`Atlas.jsx` 和 `GraphView.jsx` 中各自独立拉取，且无缓存。前端路由切换时可能导致 3 次完全相同的网络请求。~~

> [!success] 已修复
> 新增 `publishData.js` 共享缓存模块，`Note`、`GraphView`、`MiniGraph` 复用同一份图谱数据。

---

### [M6] `build-graph.mjs`：职责过多

**文件：** `scripts/build-graph.mjs`（约 580 行）

这一个脚本承担了 5 项不同职责：
1. Markdown 文件扫描器
2. frontmatter 解析器
3. Wikilink 索引器
4. 图片处理器
5. JSON 输出生成器

难以测试或调试其中任何一个独立步骤。

### [M7] ~~`BlogOld.jsx` 仍存在于仓库中~~ ✅ 已修复

~~整个旧版博客组件（文件大小 114K token）仍占用仓库空间。~~

> [!success] 已修复
> 已删除 `BlogOld.jsx` 及未使用的依赖（matter-js、@types/matter-js）、死代码 `TextPressure.jsx`。

### [M8] ~~端口号和 Vault 路径硬编码~~ ✅ 已修复

| 文件 | 硬编码内容 |
|------|------------|
| `build-graph.mjs:17` | `VAULT_PATH = C:\Users\Yhx06\Documents\Obsidian Vault` |
| `publish-admin.mjs:11` | `PORT` |

~~换机器或 CI 环境必然失败。~~

> [!success] 已修复
> 已改用 `.env` 环境变量管理 Vault 路径和端口配置。

---

### [M9] `publish-admin.mjs` 缺少 API 输入校验

- `POST /api/manifest` 直接写入客户端发送的 JSON，不做校验
- `POST /api/toggle` 和 `POST /api/toggle-batch` 同样信任所有输入

---

## 🟢 四、低危问题 (LOW)

### [L1] ~~向客户端暴露错误调用栈~~ ✅ 已修复

**文件：** `publish-admin.mjs:1360`

```javascript
send(res, 500, { error: error.message, stack: error.stack });
```

~~生产环境中不应向客户端暴露调用栈信息。~~

> [!success] 已修复
> 已移除生产环境错误响应中的 `stack` 字段。

### [L2] ~~`console.error` 被用作异常处理~~ ✅ 已修复

~~`src/` 中共有 8 处 `console.log/error/warn` 调用。多处将 `console.error` 作为唯一的错误处理手段（如 `Note.jsx:797`、`MiniGraph.jsx`），对用户毫无反馈。~~

> [!success] 已修复
> 9 处全部改为 `console.warn` 加模块标签（ErrorBoundary 保留）。

### [L3] 部分 3D 组件缺少 `React.memo`

`BlogPlanet`、`PlanetSurface`、`Lanyard` 等组件未使用 `React.memo`，结合未做限流的 `useFrame` 调用，容易导致不必要的重渲染。

### [L4] Blog.jsx 中 Scene 配置内联

7 个场景的背景、热点、视差参数等均以大型内联对象定义在组件内部，而非提取到独立的配置文件。

### [L5] 测试数量为零

`package.json` 中存在 `@testing-library/jest-dom`、`@testing-library/react`、`@testing-library/user-event`，且存在 `npm test` 脚本，但 `src/` 下没有任何测试文件。

### [L6] ~~`.vscode/tasks.json` 中残留调试配置~~ ✅ 已确认

~~单个 VS Code 任务引用 `npm start`，属于个人开发环境配置，不应提交到仓库。~~

> [!success] 判定结果
> 经核实为合理配置，保留在仓库中。

---

## 五、修复优先级排期

| 优先级 | 编号 | 修复项 | 预计工作量 | 影响面 | 状态 |
|:--:|------|------|:--:|------|:--:|
| ~~P0~~ | ~~C1 + C2~~ | ~~管理面板路径遍历 + 鉴权~~ | ~~2h~~ | 安全漏洞 | ✅ |
| **P0** | C3 | 拆分 Blog.jsx | 4-6h | 后续所有开发 | ⬜ |
| **P1** | H1 + H2 | Mermaid 安全 + 审查 dangerouslySetInnerHTML | 1h | XSS 防护 | ⬜ |
| ~~P1~~ | ~~M1~~ | ~~提取共享 frontmatter 解析逻辑~~ | ~~2h~~ | 减少重复 | ✅ |
| ~~P1~~ | ~~M2~~ | ~~添加 Error Boundaries~~ | ~~1h~~ | 防止白屏 | ✅ |
| ~~P2~~ | ~~M3 + M4~~ | ~~懒加载 + 按需加载图片~~ | ~~3h~~ | 首屏性能 | ✅ |
| ~~P2~~ | ~~H3~~ | ~~拆分内联管理面板 HTML~~ | ~~4h~~ | 管理面板可维护 | ✅ |
| **P2** | M5 | 添加图谱数据缓存 | 2h | 减少重复请求 | ⬜ |
| ~~P2~~ | ~~M8 + L1~~ | ~~环境变量 + 错误栈清理~~ | ~~1h~~ | 配置安全 | ✅ |
| ~~P3~~ | ~~L2~~ | ~~console.error 残骸清理~~ | ~~0.5h~~ | 代码质量 | ✅ |
| **P3** | L5 | 从关键路径开始添加测试 | 持续 | 质量保障 | ⬜ |
| ~~P3~~ | ~~M7 + L6~~ | ~~仓库清理（死代码、配置残留）~~ | ~~0.5h~~ | 仓库整洁 | ✅ |

> 已完成 9 项任务（11 个问题），剩余 **11 个问题**待处理。

---

## 六、关联文档

- ~~问题总录~~ — 早期版本已删除，内容已全部整合入本报告
- [[博客页-重制版/松果屋重构设计书]] — 全站重构企划
- [[博客页-重制版/博客页重制说明书v0.1]] — Blog.jsx 重构设计方案
- [[开发任务清单]] — 当前开发任务

---

> [!summary] 一句话总结
> 网站创意和视觉出色。**安全漏洞（C1+C2）和管理面板（H3）已修复，性能优化（M3+M4 懒加载）已落地。** 当前最大遗留问题是 Blog.jsx 巨型组件拆分（C3），其余 10 项均为中低优先级。

---

### 修复成果一览

| 轮次 | 修复项 | 收益 |
|:--:|------|------|
| 第一轮 | M7, L1, M2, M1 | 死代码清理、错误栈屏蔽、Error Boundary 全局覆盖、frontmatter 去重 |
| 第二轮 | M8, M2 细化 | 环境变量化、3D 组件局部保护 |
| 第三轮 | C1, C2, H3, M3+M4, L2, L6 | **路径遍历修复、管理面板鉴权、SPA 拆分、懒加载、console 规范化** |

**安全收益**：路径遍历已封堵 + 管理面板仅本机可访问 + 错误栈不再泄露 + Mermaid 改为 strict
**性能收益**：首屏拆为 7 个 chunk，3D 库按需加载 + graph.json 共享缓存消除重复请求

---

## 第四轮修复（新增问题，不在原 22 项内）

本轮发现并解决了一批部署层面和数据完整性问题。这些问题较轻，但直接影响用户体验。

| 编号 | 问题 | 修复 | 状态 |
|:--:|------|------|:--:|
| N1 | SPA 刷新 404：Cloudflare Pages 直刷 `/blog`、`/note/...`、`/graph` 返回 404 | 添加 `public/_redirects` | ✅ |
| N2 | workshop 蓝图 `articleSlug` 指向不存在的旧路径 | 改为存在的 `博客页-废弃方案/...v0.2` | ✅ |
| N3 | archive 最近更新直接取 `notesData.slice(0, 15)`，不准确 | 改为按 `updatedAt`/`date`/`createdAt` 排序 | ✅ |
| N4 | `notes-index.json` / `graph.json` 缺少 `createdAt`/`updatedAt` 时间字段 | `build-graph.mjs` 输出时间字段，补齐 public 数据 | ✅ |
| N5 | `Blog.jsx`、`Note.jsx` BOM lint warning | 移除 BOM 字符 | ✅ |
| N6 | workshop 单篇文章阅读器显示 travel 的元信息 | 改为读取 `item.articleMeta` | ✅ |
| N7 | 旅行弹窗移动端 `min-height` 覆盖实际高度 | 添加移动端高度约束 | ✅ |
| N8 | BrowserRouter 依赖部署平台 fallback | 已用 `_redirects` 补 Cloudflare Pages；换平台需对应配置 | ⬜ |
| N9 | Browserslist 数据过旧 | 构建 warning，不影响运行 | ⬜ |
| N10 | `@mediapipe` source map 缺失 | 第三方包 warning，不影响运行 | ⬜ |

> 第四轮：7 项已修，3 项低优待处理。

*创建于 2026-05-27，基于对 personal-blog 代码库的完整审查。*
